# Scripts Folder

Place one script folder per script here.

Example:

scripts/
  backup-database/
    backup.sh
    config.json
    README.md

Notes:
- Each script folder should contain at least one executable file.
- Optional metadata files include config.json, README.md, and icon.png.
